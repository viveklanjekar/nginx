http {
    server {
        listen 80;

        location /health {
            root /usr/share/nginx/html;
            try_files /health.html =404;
        }

        # Other server configurations...
    }

    # Other http configurations...
}

# Global settings outside http block (e.g., events, worker processes)
events {}
